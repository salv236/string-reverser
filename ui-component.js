function simple_elevator_handler() {
	// read user input from dom

	var result = simple_elevator(floor, button);

	// write user input to dom
}

function render_simple_elevator(container) {
	// grab container from dom

	// build all necessary dom elements

	// append them how they should be

	// attach event listeners
}

render_simple_elevator("simple-elevator");